import { Routes } from '@angular/router';
import { PollComponent } from './poll/poll.component';

export const routes: Routes = [
    

{path: '', title: 'Home Component', component: PollComponent}


];
